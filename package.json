{
  "name": "wealth-management",
  "version": "1.0.0",
  "description": "Financial Transaction Management System for Family Office",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:local": "wrangler d1 execute transactions_db --local --command",
    "db:remote": "wrangler d1 execute transactions_db --remote --command",
    "db:migrate:local": "wrangler d1 execute transactions_db --local --file=./schema.sql",
    "db:migrate:remote": "wrangler d1 execute transactions_db --remote --file=./schema.sql",
    "type-check": "tsc --noEmit"
  },
  "keywords": [
    "cloudflare",
    "workers",
    "d1",
    "finance",
    "reporting"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "hono": "^4.0.0",
    "xlsx": "https://cdn.sheetjs.com/xlsx-0.20.1/xlsx-0.20.1.tgz",
    "date-fns": "^3.0.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241127.0",
    "@types/node": "^20.11.0",
    "typescript": "^5.3.3",
    "wrangler": "^4.50.0"
  }
}
